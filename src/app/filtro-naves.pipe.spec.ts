import { FiltroNavesPipe } from './filtro-naves.pipe';

describe('FiltroNavesPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroNavesPipe();
    expect(pipe).toBeTruthy();
  });
});
