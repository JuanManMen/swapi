import { FiltroVehiculosPipe } from './filtro-vehiculos.pipe';

describe('FiltroVehiculosPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroVehiculosPipe();
    expect(pipe).toBeTruthy();
  });
});
