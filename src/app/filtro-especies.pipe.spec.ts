import { FiltroEspeciesPipe } from './filtro-especies.pipe';

describe('FiltroEspeciesPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroEspeciesPipe();
    expect(pipe).toBeTruthy();
  });
});
