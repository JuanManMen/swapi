import { FiltroPlanetasPipe } from './filtro-planetas.pipe';

describe('FiltroPlanetasPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroPlanetasPipe();
    expect(pipe).toBeTruthy();
  });
});
